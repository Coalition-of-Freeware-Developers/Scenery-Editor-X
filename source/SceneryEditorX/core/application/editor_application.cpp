/**
* -------------------------------------------------------
* Scenery Editor X
* -------------------------------------------------------
* Copyright (c) 2025 Thomas Ray
* Copyright (c) 2025 Coalition of Freeware Developers
* -------------------------------------------------------
* editor_application.cpp
* -------------------------------------------------------
* Created: 22/6/2025
* -------------------------------------------------------
*/
//#include "Editor/core/editor.h"
//#include "application/application.h"
//#include "SceneryEditorX/renderer/renderer.h"
//#include "SceneryEditorX/ui/ui.h"
//#include "SceneryEditorX/ui/ui_context.h"

/// -------------------------------------------------------

namespace SceneryEditorX
{/*

    EditorApplication::EditorApplication()
    {
        // Initialize member variables
    }

    EditorApplication::~EditorApplication()
    {
        // Clean up resources
    }

    void EditorApplication::InitEditor()
    {
        // Initialize the editor components

        // Initialize the renderer
        Renderer::Init();

        // Initialize other components like UI, scene, etc.
    }

    void EditorApplication::Run()
    {
        // Run the editor application

        // Initialize the editor
        InitEditor();

        // Start the main loop
        MainLoop();
    }

    void EditorApplication::Update()
    {
        // Update the editor state

        // Check if window has been resized
        // If so, recreate frame resources
        if (/* window resized condition #1#)
        {
            RecreateFrameResources();
        }

        // Update UI, input, etc.
    }

    void EditorApplication::DrawFrame()
    {
        // Render a single frame

        // Begin frame
        Renderer::BeginFrame();

        // Render UI, scene, etc.

        // End frame
        Renderer::EndFrame();

        // Submit frame
        Renderer::SubmitFrame();
    }

    void EditorApplication::Create()
    {
        // Create necessary resources for the editor

        // Create window, setup UI, etc.
    }

    void EditorApplication::MainLoop()
    {
        // Main application loop

        // Loop until window is closed
        while (/* window not closed condition #1#)
        {
            // Update editor state
            Update();

            // Render frame
            DrawFrame();
        }
    }

    void EditorApplication::RecreateFrameResources()
    {
        // Recreate frame resources when window is resized

        // Wait for GPU to finish
        // auto device = RenderContext::Get()->GetLogicDevice();
        // vkDeviceWaitIdle(device->GetDevice());

        // Recreate swapchain, framebuffers, etc.

        // Update viewport size in UI
    }
    */

} // namespace SceneryEditorX

/// -------------------------------------------------------
