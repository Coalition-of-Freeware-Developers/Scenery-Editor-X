/**
* -------------------------------------------------------
* Scenery Editor X
* -------------------------------------------------------
* Copyright (c) 2025 Thomas Ray 
* Copyright (c) 2025 Coalition of Freeware Developers
* -------------------------------------------------------
* vk_debug.cpp
* -------------------------------------------------------
* Created: 21/6/2025
* -------------------------------------------------------
*/
//#include "debug_renderer.h"

/// ---------------------------------------------------------

/*
namespace SceneryEditorX
{

	void DebugRenderer::DrawLine(const Vec3& p0, const Vec3& p1, const Vec4& color, bool onTop)
	{
		m_RenderQueue.emplace_back([p0, p1, color, onTop](const Ref<Renderer2D> &renderer)
		{
			renderer->DrawLine(p0, p1, color, onTop);
		});
	}

	void DebugRenderer::DrawCircle(const Vec3& centre, const Vec3& rotation, float radius, const Vec4& color, bool onTop)
	{
		m_RenderQueue.emplace_back([centre, rotation, radius, color, onTop](const Ref<Renderer2D> &renderer)
		{
			renderer->DrawCircle(centre, rotation, radius, color, onTop);
		});
	}

	void DebugRenderer::DrawTransform(const Mat4& transform, float scale, bool onTop)
	{
		m_RenderQueue.emplace_back([transform, scale, onTop](const Ref<Renderer2D> &renderer)
		{
			renderer->DrawTransform(transform, scale, onTop);
		});
	}

	void DebugRenderer::DrawQuadBillboard(const Vec3& translation, const Vec2& size, const Vec4& color)
	{
		m_RenderQueue.emplace_back([translation, size, color](const Ref<Renderer2D> &renderer)
		{
			renderer->DrawQuadBillboard(translation, size, color);
		});
	}

	void DebugRenderer::SetLineWidth(float thickness)
	{
		m_RenderQueue.emplace_back([thickness](const Ref<Renderer2D> &renderer)
		{
			renderer->SetLineWidth(thickness);
		});
	}

}
*/

/// ---------------------------------------------------------
